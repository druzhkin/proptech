#!/usr/bin/env python3
"""
Client for Anthropic Claude API.

Two functions:
- filter_articles: selects top N articles via Claude with strict rejection criteria
- generate_post: generates a Telegram post with validation loop and retry
"""

import json
import logging
import re

import anthropic

logger = logging.getLogger(__name__)

FILTER_SYSTEM = """Ты фильтруешь новости для экспертного Telegram-канала PropTech Russia.
Аудитория — CTO застройщиков, коммерческие директора, руководители продукта.
Это жёсткие профессионалы. Им нельзя подсовывать пустышки.

═══ КРИТЕРИИ ОТБОРА ═══

Статья ПРОХОДИТ если:
- Есть конкретное внедрение у реальной компании
- Есть измеримый результат: цифры производительности, экономии, скорости
- Технология применима к жилому девелопменту
- ИЗ СТАТЬИ МОЖНО НАПИСАТЬ ИНТЕРЕСНЫЙ ПОСТ — есть техническая суть,
  а не просто "компания привлекла деньги / получила награду"

═══ ОБЯЗАТЕЛЬНО ОТКЛОНИ ═══

- Нет НИ ОДНОГО реального внедрения — только планы, патенты, прототипы
- Единственная значимая информация — сумма раунда/инвестиций
- Единственный "клиент" — пилот без результатов
- AI-washing: обычный продукт с приклеенным словом "AI"
- Проект "существует на бумаге" или "планируется к запуску"
- Коммерческая недвижимость без связи с жилым строительством
- Статья — список наград/номинаций без технических деталей
- Статья — обзор рынка / прогноз / абстрактный тренд без конкретного продукта

═══ БАЛАНС ═══

- Не больше 2 из одной категории
- Минимум 1 из Азии (если достойная)
- Минимум 1 про маркетинг/продажи/визуализацию (если достойная)
- Не больше 1 про одну компанию

═══ ФОРМАТ ═══

ТОЛЬКО JSON, без обёрток:
[{"id": "uuid", "reason": "одно предложение"}]

Если достойных меньше 5 — верни сколько есть. Лучше 2 сильных чем 5 слабых.
Можно вернуть []."""


GENERATE_SYSTEM = """Ты пишешь пост для Telegram-канала PropTech Russia от лица Даниила — 15 лет аналитическим директором в жилом девелопменте, создал 8 AI-инструментов.

═══ ГЛАВНОЕ ПРАВИЛО ═══

Пиши как умный мужик в чате объясняет коллеге за обедом. НЕ как журналист. НЕ как пресс-релиз. НЕ как корпоративный блог.

Тест: если предложение можно вставить в пресс-релиз компании — перепиши его. Если звучит как разговор — оставь.

═══ ПРИМЕРЫ ХОРОШИХ ПОСТОВ ═══

ПРИМЕР 1:
---
📌 3000 планировок за 3 секунды на обычном ноутбуке

TestFit выкатили генеративный дизайн — задаёшь участок, этажность, нормы парковки, и система перебирает тысячи вариантов компоновки. Не ML, не нейросети — чистый алгоритмический перебор с оптимизацией по FAR, yield on cost, количеству юнитов.

Для кого это: девелоперы multifamily, которые оценивают участки. Вместо архитектора, который за 2 недели нарисует 3 варианта — получаешь 3000 за минуту. Потом выбираешь лучший и отдаёшь в Revit.

Ограничение честное: эстетику не оценивает. Это калькулятор, не архитектор. Но на этапе feasibility study — убирает неделю ручной работы.

Лицензия от $500/мес. При пяти площадках в проработке — окупается за первый месяц.

🎥 Демо: https://youtube.com/...
Источник: https://engineering.com/...

#PropTech #AI #GenDesign
---

ПРИМЕР 2:
---
📌 Камера на каске заменяет прораба — отклонение от графика видно в тот же день

Buildots вешает 360° камеру на каску обходчика. Он идёт по объекту как обычно — камера снимает. Потом софт накладывает фото на BIM-модель и красит красным всё что отстаёт от плана.

Звучит просто, но дьявол в деталях: нужен актуальный BIM (не тот что сдали на экспертизу 2 года назад, а рабочий). Skanska и Zublin используют на десятках объектов.

Заявляют 3-4 часа экономии в неделю на одного project manager. По деньгам — если PM стоит $80/час, это $15K/год на человека. Лицензия, подозреваю, сопоставима.

Но: IFC/Revit only. Если у вас BIM на бумажке — не взлетит.

Источник: https://buildots.com/...

#PropTech #AI #ConTech
---

ПРИМЕР 3:
---
📌 Китайский Zillow обрабатывает 2 млрд запросов в день — у нас такого нет даже близко

Beike (KE Holdings) — крупнейшая платформа недвижимости в Китае. 320 млн обработанных листингов. AI подбирает квартиры, оценивает стоимость, генерирует 3D-туры из фотографий.

Самое интересное — Realsee: их движок делает VR-тур из обычных фото смартфоном. Не Matterport с дорогой камерой — буквально телефон + их алгоритм = 3D модель квартиры.

Масштаб поражает: 20 000+ агентств подключены, GMV $255 млрд/год. Это не стартап — это инфраструктура целого рынка.

Источник: https://investors.ke.com/...

#PropTech #AI #VirtualStaging
---

ПРИМЕР 4:
---
📌 Intuit запустил ERP для застройщиков — AI сам ловит перерасход и кассовые разрывы

Intuit Enterprise Suite Construction Edition — первая ERP от создателей QuickBooks, заточенная под жилое строительство среднего размера. Управление проектами, бюджеты по фазам, отслеживание по категориям.

AI анализирует движение денег и автоматически выявляет аномалии: где бюджет течёт, где будет кассовый разрыв, какие проекты тормозят. По сути, финдиректор-бот, который работает 24/7 и знает каждую копейку.

Для кого: компании 10-50 домов в год, которые выросли из QuickBooks, но до Oracle NetSuite не доросли. Где Excel уже не справляется с cash flow, а SAP избыточен.

Засада честная: миграция и переобучение — месяцы. И AI работает настолько хорошо, насколько хороша ваша история проектов. Если бюджеты вели на коленке — система будет выдавать мусор.

За 15 лет видел десятки ERP-внедрений в девелопменте. 70% проваливались не из-за софта, а из-за людей — продолжали работать по-старому, обходя систему. Intuit хотя бы понимает строительную специфику.

Источник: https://martechedge.com/...

#PropTech #AI #ConTech
---

═══ ЧТО ДЕЛАЕТ ПОСТ ХОРОШИМ ═══

1. Хук = результат или провокация, 5-12 слов
2. Объясняет КАК РАБОТАЕТ простым языком, не маркетинг
3. Считает деньги: лицензия, экономия, ROI — хотя бы прикидку
4. Говорит ЧТО НЕ РАБОТАЕТ — честно
5. Имеет позицию: "это полезно потому что..." или "это хайп потому что..."
6. Звучит как человек, не как бот

═══ ЧТО ДЕЛАЕТ ПОСТ ПЛОХИМ ═══

1. Начинается с "[Компания] из [город] создала/привлекла/представила..."
2. Сумма раунда как главная новость
3. "Платформа работает как AI-инфраструктурный слой" — корпоративный жаргон
4. "Система анализирует данные и выдаёт результат" — ну а что ещё AI делает?
5. Нет позиции — просто пересказ без мнения
6. Все предложения одинаковой длины, формальный тон
7. "Звучит как научная фантастика" / "это меняет всё" — клише, не используй
8. Слишком длинный — больше 5 абзацев = стена текста, люди скроллят мимо
9. Три упоминания суммы раунда в одном посте — значит пост всё ещё про деньги

═══ СТРУКТУРА ═══

📌 [Хук: 5-12 слов. Результат или провокация]

[2-3 абзаца свободным текстом. Не по шаблону "Результат: ... Кто использует: ...". Естественный поток: что это → как работает → для кого → ограничения. Порядок может меняться. Где уместно — ирония, сравнения, прикидки по деньгам]

[Если уместно — личное мнение или контекст от первого лица. "Мы пробовали похожее...", "За 15 лет видел десяток таких попыток..."]

Источник: [ссылка]

#PropTech #AI #[тег]

═══ ПРАВИЛА ═══

- 200-350 слов. Telegram — короткий формат. 350 слов = абсолютный максимум. Лучше 250-300
- Русский язык. Англицизмы ок (BIM, IoT, LiDAR, AVM, ML)
- Эмодзи: ТОЛЬКО 📌💡🤖🎥
- Раунд инвестиций — максимум одно упоминание, не в хуке, не как главная новость.
  Если кроме раунда нечего сказать — пост не нужен
- Если цифра сомнительная — "по их словам", "заявляют", "если верить компании"
- НЕ НАЧИНАЙ с названия компании. Начинай с результата
- ОДИН пост = ОДНА компания
- НЕ ПИШИ: "а в России", "на российском рынке", "у нас пока"
- НЕ ПИШИ: уникальный, эксклюзивный, революционный, инновационный,
  передовой, прорывной, не имеет аналогов

═══ ПРО СУММУ РАУНДА ═══

Если в статье главное — привлечение денег, перефокусируй на продукт:
- Плохо: "Bedrock Robotics привлекла $270M Series B"
- Хорошо: "Автономный экскаватор — ставишь комплект датчиков за 4 часа,
  машина копает сама. $270M от CapitalG и NVIDIA говорят что рынок поверил"

Деньги — контекст, не новость. CTO застройщика не покупает акции Bedrock."""


def filter_articles(articles: list[dict], max_select: int = 5) -> list[dict]:
    """
    Claude выбирает лучшие статьи из списка.
    Возвращает список {"id": ..., "reason": ...}.
    При ошибке парсинга — retry 1 раз. При повторной ошибке — возвращает все статьи.
    """
    client = anthropic.Anthropic()

    # Краткие описания для Claude (не гнать полный текст 10 статей)
    summaries = []
    for a in articles:
        summaries.append({
            "id": a["id"],
            "title": a["title"],
            "source_name": a["source_name"],
            "source_type": a["source_type"],
            "category_hint": a.get("category_hint", "unknown"),
            "text_preview": a["text"][:1500],  # достаточно для оценки
        })

    user_msg = (
        f"Отбери до {max_select} лучших статей. "
        f"Строго примени критерии — не пропускай мусор.\n\n"
        f"{json.dumps(summaries, ensure_ascii=False, indent=2)}"
    )

    for attempt in range(2):  # максимум 2 попытки
        try:
            message = client.messages.create(
                model="claude-sonnet-4-20250514",
                max_tokens=2000,
                system=FILTER_SYSTEM,
                messages=[{"role": "user", "content": user_msg}],
            )

            response_text = message.content[0].text.strip()

            # Убрать markdown-обёртку если Claude всё-таки добавил
            if response_text.startswith("```"):
                response_text = response_text.split("\n", 1)[1]
                response_text = response_text.rsplit("```", 1)[0].strip()

            result = json.loads(response_text)

            # Валидация: результат должен быть списком словарей с полем "id"
            if not isinstance(result, list):
                raise ValueError("Ответ не массив")
            for item in result:
                if "id" not in item:
                    raise ValueError(f"Нет поля id: {item}")

            logger.info(
                "Фильтрация: выбрано %d из %d (попытка %d)",
                len(result), len(articles), attempt + 1,
            )
            # Логируем стоимость
            usage = message.usage
            logger.info(
                "Tokens: input=%d, output=%d",
                usage.input_tokens, usage.output_tokens,
            )

            return result

        except (json.JSONDecodeError, ValueError, KeyError) as e:
            logger.warning("Ошибка парсинга фильтра (попытка %d): %s", attempt + 1, e)
            if attempt == 0:
                continue
            else:
                # Fallback: вернуть все статьи без фильтрации
                logger.warning("Фильтрация не удалась, используем все статьи")
                return [
                    {"id": a["id"], "reason": "фильтрация не сработала"}
                    for a in articles[:max_select]
                ]

        except anthropic.APIError as e:
            logger.error("Anthropic API error: %s", e)
            return [
                {"id": a["id"], "reason": "API недоступен"}
                for a in articles[:max_select]
            ]

    return []


def generate_post(article: dict) -> str | None:
    """
    Генерирует один пост. При проблемах с валидацией — retry до 2 раз
    с указанием конкретных ошибок.
    """
    client = anthropic.Anthropic()

    article_context = (
        f"Источник: {article['source_name']} ({article['source_type']})\n"
        f"Заголовок: {article['title']}\n"
        f"URL: {article['url']}\n\n"
        f"Полный текст:\n{article['text'][:5000]}"
    )

    messages = [{"role": "user", "content": f"Напиши пост:\n\n{article_context}"}]

    for attempt in range(3):  # до 3 попыток
        try:
            message = client.messages.create(
                model="claude-sonnet-4-20250514",
                max_tokens=1500,
                system=GENERATE_SYSTEM,
                messages=messages,
            )

            post_text = message.content[0].text.strip()
            usage = message.usage
            logger.info(
                "Генерация (попытка %d): %d+%d tokens",
                attempt + 1, usage.input_tokens, usage.output_tokens,
            )

            # Валидация
            problems = validate_post(post_text)

            if not problems:
                return post_text

            if attempt < 2:
                # Retry с указанием конкретных проблем
                logger.info("Валидация не пройдена: %s. Retry...", problems)
                messages = [
                    {"role": "user", "content": f"Напиши пост:\n\n{article_context}"},
                    {"role": "assistant", "content": post_text},
                    {
                        "role": "user",
                        "content": (
                            "Исправь пост. Конкретные проблемы:\n"
                            + "\n".join(f"- {p}" for p in problems)
                            + "\n\nПерепиши пост целиком с исправлениями."
                        ),
                    },
                ]
            else:
                # Третья попытка не удалась — возвращаем как есть с пометкой
                logger.warning("Валидация не пройдена после 3 попыток: %s", problems)
                return post_text

        except anthropic.APIError as e:
            logger.error("Anthropic API error: %s", e)
            if attempt < 2:
                continue
            return None

    return None


def validate_post(text: str) -> list[str]:
    """
    Проверяет пост на соответствие правилам.
    Возвращает список проблем. Пустой список = всё ок.
    """
    problems = []
    words = text.split()
    word_count = len(words)

    # Длина
    if word_count < 200:
        problems.append(f"Слишком короткий: {word_count} слов, нужно минимум 250")
    if word_count > 400:
        problems.append(f"Слишком длинный: {word_count} слов, максимум 350")

    # Хук
    if not text.startswith("\U0001f4cc"):  # 📌
        problems.append("Пост должен начинаться с 📌")
    else:
        # Проверить длину хука (первая строка)
        first_line = text.split("\n")[0]
        hook_words = len(first_line.split())
        if hook_words > 20:
            problems.append(f"Хук слишком длинный: {hook_words} слов, максимум 15")

    # Запрещённые слова
    forbidden = [
        "уникальн", "эксклюзивн", "не имеет аналогов",
        "революционн", "инновационн", "передовой", "прорывн",
        "беспрецедентн", "впервые в мире",
        "а в россии", "на российском рынке", "у нас пока",
    ]
    text_lower = text.lower()
    for word in forbidden:
        if word in text_lower:
            problems.append(f"Запрещённое слово/фраза: '{word}'")

    # Источник
    if "источник:" not in text_lower and "source:" not in text_lower:
        problems.append("Нет ссылки на источник (строка 'Источник: URL')")

    # Теги
    if "#PropTech" not in text and "#proptech" not in text.lower():
        problems.append("Нет тега #PropTech")

    # Запрещённые эмодзи
    allowed = {"\U0001f4cc", "\U0001f4a1", "\U0001f916", "\U0001f3a5"}  # 📌💡🤖🎥
    # Находим все эмодзи в тексте
    emoji_pattern = re.compile(
        "[\U0001F300-\U0001F9FF"   # Misc Symbols and Pictographs + Emoticons + etc
        "\U00002600-\U000027BF"    # Misc symbols
        "\U0001FA00-\U0001FA6F"    # Chess Symbols
        "\U0001FA70-\U0001FAFF"    # Symbols and Pictographs Extended-A
        "\U00002702-\U000027B0"    # Dingbats
        "]+",
        flags=re.UNICODE,
    )
    found_emoji = set(emoji_pattern.findall(text))
    # Разбиваем на отдельные символы
    all_emoji = set()
    for e in found_emoji:
        all_emoji.update(e)
    bad_emoji = all_emoji - allowed
    if bad_emoji:
        problems.append(
            f"Запрещённые эмодзи: {''.join(bad_emoji)}. Разрешены только: 📌💡🤖🎥"
        )

    # Несколько компаний (грубая проверка)
    multi_signals = ["параллельно", "отдельно:", "также стоит отметить", "кроме того,"]
    for signal in multi_signals:
        if signal in text_lower:
            problems.append(
                f"Похоже на пост-сборник (найдено '{signal}'). Один пост = одна компания"
            )

    return problems
